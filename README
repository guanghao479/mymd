## Install & Run ##
$ pwd
......../mymd
$ cd ..

# Install necessary library on Ubuntu
$ sudo apt-get install libjpeg libjpeg-dev libfreetype6 libfreetype6-dev zlib1g-dev

# Install virtualenv and pip
$ sudo apt-get install python-pip python-dev build-essential 
$ sudo pip install --upgrade pip 
$ sudo pip install --upgrade virtualenv

$ virtualenv mymd-env
$ source mymd-env/bin/activate
(mymd-env)$ pip install -U -r mymd/requirements/project.txt

# Now you can run the project as normal Django project
(mymd-env)$ python manage.py syncdb
(mymd-env)$ python manage.py runserver